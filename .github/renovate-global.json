{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": [
    "Global (self-hosted admin) config for the central Renovate runner — NOT this repo's own",
    "dependency config (that stays in .github/renovate.json). Loaded by .github/workflows/",
    "renovate.yml; see its header comment for the full auth/token-scope rationale."
  ],
  "extends": ["config:recommended", ":semanticCommits"],
  "autodiscover": true,
  "onboarding": false,
  "requireConfig": "optional",
  "allowedCommands": ["^scripts/refresh-binary-checksums\\.sh$"]
}
